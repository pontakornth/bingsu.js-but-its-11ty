<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/base.css">
    <title>Bingsu.JS</title>
</head>
<body>
<main class="container mx-auto">
<h1 class="text-indigo-500 text-6xl py-4">Bingsu.js</h1>
{%- for datum in bingsu | sort(true, false, "date") -%}
 <h2 class="text-indigo-400 text-5xl py-4">{{ datum.name  }}</h2>
 <span>Date: {{ datum.date  }} </span>
 <h3>Parties</h3>
 {%- for party in datum.party -%}
    <ul class="list-disc">
        <li class="ml-4"><span class="text-indigo-600 font-bold">Name:</span> {{ party.name }}</li>
        <li class="ml-4"><span class="text-indigo-600 font-bold">Place:</span> {{ party.place }}</li>
        <li class="ml-4"><span class="text-indigo-600 font-bold">Url:</span> <a href={{ party.url }} target="__blank">{{ party.url }}</a></li>
    </ul>
    {%- endfor -%}
{%- endfor -%}
</main>
</body>
</html>